<manifest
    package="com.project.movienearme"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.AddMovieActivity"/>
        <activity android:name=".activities.MovieDetailsCinemaActivity"/>
        <activity android:name=".activities.MovieDetailsTimeActivity"/>
        <activity android:name=".activities.SeatSelectionActivity"/>
    </application>

</manifest>
